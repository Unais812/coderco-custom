name: coderco custom action ci

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v2

    - name: hello world custom action 
      uses: Unais812/coderco-custom@main
      with:
        who-to-greet: 'Coderco Team team'