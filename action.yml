name: Coderco custom action 

description: greet someone

inputs:
  who-to-greet:
    description: who to greet
    required: true
    default: hello

outputs:
  time:
    description: time greeted

runs:
  using: node20
  main: dist/index.js